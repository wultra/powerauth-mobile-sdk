name: Android Build

on:
  push:
    branches:
      - develop
      - master
      - releases/*
      - feature/crypto4
    paths:
      - "cc7"
      - "src/PowerAuth/**"
      - "src/PowerAuthJni/**"
      - "src/PowerAuthTests/**"
      - "include/**"
      - "proj-android/**"
  pull_request:
    paths:
      - "cc7"
      - "src/PowerAuth/**"
      - "src/PowerAuthJni/**"
      - "src/PowerAuthTests/**"
      - "include/**"
      - "proj-android/**"

jobs:
  build:
    name: Build Android platform
    runs-on: ubuntu-latest
    steps:
      - name: Checkout the repo
        uses: actions/checkout@v3
        with:
            submodules: 'recursive'
      - name: Setup Java 17
        uses: actions/setup-java@v3
        with:
          distribution: 'temurin'
          java-version: '17'
          cache: 'gradle'
      - name: Build library
        run: ./scripts/test-build.sh android -v2